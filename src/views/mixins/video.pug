mixin video(video)
    div 
        h4
            a(href=`/videos/${video.id}`)=video.title
            //href나 class 혹은 id 같은 arttribute에는 `${}`을 사용해야한다.
        p=video.description
        each hashtag in video.hashtags
            li=hashtag
        small=video.createdAt
        hr