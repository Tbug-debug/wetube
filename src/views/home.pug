extends base
include mixins/video

block upload
    nav
        ul 
            li 
                a(href="videos/upload") Upload

block content
    each fish in videos
        +video(fish)
    else 
        li Sorry nothing found.