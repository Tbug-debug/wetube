extends base
include mixins/video

block upload
    nav
        ul 
            li 
                a(href="videos/upload") Upload
            li 
                a(href="/search") Search
            li 
                a(href="/join") Join


block content
    each fish in videos
        +video(fish)
    else 
        li Sorry nothing found.