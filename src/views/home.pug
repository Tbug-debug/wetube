extends base
include mixins/video

block upload
    nav
        ul 
            li 
                a(href="videos/upload") Upload
            li 
                a(href="/search") Search


block content
    each fish in videos
        +video(fish)
    else 
        li Sorry nothing found.